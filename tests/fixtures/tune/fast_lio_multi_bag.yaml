# generated by bagx TEST on TIMESTAMP
#
# Starting-point parameters inferred from bag metrics.
# Not a guarantee of SLAM performance — review before deployment.
#
#   lidar_topic: /lidar
#   imu_topic: /imu
#   accel_noise_density: 0 m/s²
#   gyro_noise_density: 0 rad/s
#   accel_bias_instability: 0 m/s²
#   gyro_bias_instability: 0 rad/s
#   worst_lidar_imu_sync_delay: 25.622 ms
#
common:
    lid_topic: "/lidar"
    imu_topic: "/imu"
    time_sync_en: false  # bagx: not estimated, framework default
    time_offset_lidar_to_imu: 0.0  # bagx: not estimated, framework default

preprocess:
    lidar_type: 2  # bagx: not estimated, framework default
    scan_line: 16  # bagx: not estimated, framework default
    blind: 4  # bagx: not estimated, framework default

mapping:
    acc_cov: 0.0
    gyr_cov: 0.0
    b_acc_cov: 0.0
    b_gyr_cov: 0.0
    fov_degree: 360  # bagx: not estimated, framework default
    det_range: 100.0  # bagx: not estimated, framework default

# bagx: LiDAR↔IMU sync delay 25.6ms — enable per-point deskew / timestamp compensation
